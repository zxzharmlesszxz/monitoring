#!/usr/bin/env php
<?php
/**
 * Created by PhpStorm.
 * User: harmless
 * Date: 15.06.17
 * Time: 15:47
 */
define('MONENGINE', 'Remake by starky');
require_once __DIR__ . "/../include/core.php";

$items = array();
$query = db()->query("SELECT * FROM " . DB_SERVERS);

while ($r = db()->fetch_array($query)) {
    $items[] = $r;
}

$redis = new Redis();
$redis->connect($settings['redis_host']);
$redis->auth($settings['redis_password']);
$redis->select(1);
$redis->set('message', 'Hello world');
foreach ($items as $item)
{
    $redis->hSet('servers', $item['server_id'], serialize($item));
}
var_dump($redis->get('message'));
print_r($redis->hGetAll('servers'));